import type { Metadata } from "next";
export const metadata: Metadata = { title: "Participants" };
export default function ParticipantsPage() {
    return <div className="flex items-center justify-center h-full text-ink-tertiary text-sm p-8">Participants page — coming soon</div>;
}
