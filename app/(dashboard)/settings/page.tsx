import type { Metadata } from "next";
export const metadata: Metadata = { title: "Settings" };
export default function SettingsPage() {
    return <div className="flex items-center justify-center h-full text-ink-tertiary text-sm p-8">Settings page — coming soon</div>;
}
