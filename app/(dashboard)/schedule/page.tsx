import type { Metadata } from "next";
export const metadata: Metadata = { title: "Schedule" };
export default function SchedulePage() {
    return <div className="flex items-center justify-center h-full text-ink-tertiary text-sm p-8">Schedule page — coming soon</div>;
}
